- breadcrumb :routines, @routines
.card
  .card-header
    一覧
  .card-body
    .list-group
      - @routines.each do |routine|
        = link_to routine.name, routine, class: 'list-group-item'
    .mt-3
      = link_to fa_icon('plus-circle lg', text: '追加'), new_routine_path
